SELECTの基本的な構文

  SELECT 列名
  FROM テーブル名;

  SELECT 列名
  FROM テーブル名
  WHERE 演算子を使って検索条件を指定

データを追加するINSERT文

  INSERT FROM テーブル名
  VALUES (値1,値2,……);

データを更新するUPDATE文

  UPDATE テーブル名
  SET 列名 = 値1
  WHERE 演算子を使って検索条件を指定

データを削除するDELETE文

  DELETE FROM テーブル名
  WHERE 演算子を使って検索条件を指定
